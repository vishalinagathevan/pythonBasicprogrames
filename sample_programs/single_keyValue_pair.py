#  Create a dict in single key pair value
food = {
  "furit": "apple",
}
# Used to in-build fuction 
(x,y), = food.items()
# P rint the only single key 
print(x)
print()
animal= {
  "dog": "eating",
}
#  (x,y), = animal.items()
assign =animal.keys()
print(assign)